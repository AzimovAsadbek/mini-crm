# Backend API manzili.
#
# Lokal ishlashda bu o'zgaruvchi KERAK EMAS — Vite dev serveri `/api` ni
# http://localhost:4000 ga proxy qiladi (vite.config.ts).
#
# Deploy qilinganda backendning to'liq manzilini `/api` bilan birga yozing:
# VITE_API_URL=https://mini-crm-api.vercel.app/api
